# Журнал разработки MrDevCourses — 2026-09-07

## 1. Задачи и прогресс

### 1.1. Полный аудит безопасности и соответствия законодательству (37 пунктов)

- **Аудит безопасности и Legal/UX**:
  - Проведен комплексный аудит всей платформы MrDevCourses (бэкенд Spring Boot 3 + фронтенд React 19) по 37 пунктам чеклиста (19 Legal/UX + 18 Security).
  - Результаты первичной проверки: 22 PASS, 13 FINDING, 2 N/A.
  - Выявлен 1 критический дефект: хардкод fallback-значения JWT секрета в `application.yml`.
  - Выявлены 3 дефекта категории High (отсутствие политики возврата, отсутствие чекбокса согласия на обработку персональных данных при регистрации, отсутствие реквизитов исполнителя в футере).
  - Выявлены 6 дефектов категории Medium (политика cookie, баннер cookie consent, адаптация под Закон РК № 94-V «О персональных данных и их защите», доступность модальных окон по Escape и фокусу).

### 1.2. Устранение критической уязвимости JWT секрета (#27 CRITICAL)

- **Бэкенд-конфигурация**:
  - В `backend/src/main/resources/application.yml` полностью удален дефолтный fallback секрета. Строка заменена на обязательную интерполяцию переменной окружения: `secret: ${JWT_SECRET}`.
  - В `backend/src/test/resources/application-test.yml` зафиксирован выделенный тестовый ключ (`app.jwt.secret`), гарантирующий полную автономность прогона юнит- и интеграционных тестов в CI/CD без внешних секретов.
  - В корне проекта создан `.env` (и дубликат `backend/.env`) с криптографически стойким 128-символьным шестнадцатеричным ключом (64 байта энтропии).
  - Создан шаблон `.env.example` для безопасной настройки локального окружения новыми разработчиками.
  - В `.gitignore` расширен блок игнорирования секретов: заблокированы все паттерны `.env*`, `*.env`, сертификаты и ключи (`*.pem`, `*.key`, `*.p12`, `*.jks`).

### 1.3. Юридическая документация и соответствие законам Республики Казахстан (#3, #13, #16)

- **Политика возврата средств (Refund Policy)**:
  - Создан компонент `RefundPolicyPage.tsx` (`/refund`) с регламентом 14-дневного безусловного возврата средств в соответствии с Законом РК от 4 мая 2010 года № 274-IV «О защите прав потребителей».
  - Описаны правила расчета возврата для тарифов с куратором и индивидуальным менторством, порядок подачи заявки и сроки перечисления.
  - Маршрут зарегистрирован в `ROUTES.REFUND` и в `router/index.tsx`.
- **Политика конфиденциальности и Cookies Policy**:
  - `PrivacyPage.tsx` дополнена ссылками на Закон РК от 21 мая 2013 года № 94-V «О персональных данных и их защите» и права субъекта данных (ст. 24).
  - Добавлен специализированный раздел о файлах cookie с разъяснением назначения единственного технического cookie `MrDev_token` (`httpOnly`, `Secure`, `SameSite`) и подтверждением полного отсутствия сторонних аналитических/рекламных трекеров.
- **Реквизиты исполнителя в Footer**:
  - В `Footer.tsx` добавлен блок реквизитов: Исполнитель Орынбасар Мурат (Mr Developer), Республика Казахстан, г. Шымкент, e-mail `muratorynbasar0@gmail.com`, телефон `+7 775 058 40 21`.

### 1.4. Форма согласия и Cookie Consent Banner (#12, #15)

- **Чекбокс согласия при регистрации**:
  - В `EmailAuthForm.tsx` для режима регистрации внедрен обязательный чекбокс согласия с Условиями использования и Политикой конфиденциальности.
  - Валидация блокирует отправку формы без отметки чекбокса с выводом понятного сообщения об ошибке.
- **Информационный баннер cookie (CookieConsentBanner)**:
  - Создан виджет `CookieConsentBanner.tsx` в монохромной палитре (`#0e0e11`, градации серого и белого, zero emojis).
  - Баннер информирует пользователя об использовании технических cookie, сохраняет статус согласия в `localStorage` и монтируется глобально в `App.tsx`.

### 1.5. Доступность модальных окон и контрастность (#1, #5, #18)

- **Клавиатурная навигация и ARIA**:
  - В `StudentHelpModal.tsx` и `WelcomeOnboardingModal.tsx` добавлены слушатели клавиши `Escape` для быстрого закрытия с клавиатуры.
  - Контейнеры модальных окон снабжены атрибутами `role="dialog"`, `aria-modal="true"` и корректными `aria-label`/`aria-labelledby`.
- **Контрастность типографики**:
  - Служебные подписи и даты на страницах `PrivacyPage.tsx` и `TermsPage.tsx` переведены с `zinc-500` на `zinc-400` для гарантированного соответствия WCAG AAA.

## 2. Верификация тестов и сборки

- **Backend (JUnit)**:
  - Все 250 тестов завершились успешно (`BUILD SUCCESSFUL in 1m 37s`, 6 actionable tasks).
  - Проверена изоляция JWT-конфигурации в тестовом профиле.
- **Frontend (Vitest)**:
  - Все 80 тестов в 33 тестовых наборах завершились со 100% успехом (`Test Files: 33 passed, Tests: 80 passed`).
- **Production Build (Vite & TypeScript)**:
  - `tsc -b && vite build` выполнен успешно (`built in 5.67s`, 1751 модуль трансформирован, 0 ошибок сборки).
