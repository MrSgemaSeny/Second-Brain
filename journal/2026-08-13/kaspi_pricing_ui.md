# 2026-08-13: UI подписок Kaspi Pay (1, 3, 6 месяцев)

## Сделано
- В таблицу `users` добавлено поле `subscription_expires_at` (`V12__add_subscription_expires_at.sql`).
- Обновлена сущность `User` для поддержки даты истечения подписки.
- В `KaspiPayService` добавлен расчет цены и обработка периода подписки (1, 3, 6 месяцев), который передается через `orderId` (например, `1_3`).
- Обновлен эндпоинт `/checkout/kaspi`, теперь он принимает параметр `months`.
- В `PricingPage.tsx` внедрен строгий, "грозный" UI в темных тонах (GitHub Dark Mode) с тремя карточками для Kaspi (1 месяц, 3 месяца, 6 месяцев) и их ценами (15000, 40000, 75000).
- При успешном получении вебхука от Kaspi бэкенд вычисляет `subscription_expires_at = now + months`.
